services:
  - type: web
    name: spelltris-backend
    runtime: python
    runtimeVersion: "3.11"
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 10000
